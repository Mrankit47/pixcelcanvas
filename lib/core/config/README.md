# Core Config

Environment configuration, app settings, and runtime flags for PixelCanvas per Blueprint §8.1.

## Files

- **`environment.dart`**: Environment enum (`dev`, `staging`, `prod`) and runtime configuration flags.
- **`app_config.dart`**: Singleton application configuration container holding system constants and feature flags.
