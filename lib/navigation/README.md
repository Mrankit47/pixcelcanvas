# Navigation

Declarative routing, navigation hierarchy, named routes, and route guards per Blueprint §7.

## Files

- **`app_router.dart`**: Declarative routing configuration using `go_router` (`StatefulShellRoute` for bottom nav).
- **`route_names.dart`**: Constant named routes and route paths.
- **`auth_guard.dart`**: Navigation redirect logic protecting authenticated routes.
