/// Categories for keyboard shortcuts per Blueprint §8.3.
enum ShortcutCategory {
  application,
  project,
  canvas,
  drawing,
  selection,
  layers,
  animation,
  export,
  view,
  navigation,
  developer,
}
